<UserControl x:Class="MasterYourEnglish.Presentation.Views.TestSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource App.BackgroundBrush}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{Binding ProgressText}" FontSize="16" Foreground="Gray" HorizontalAlignment="Right"/>
            <TextBlock Text="{Binding QuestionText}" FontSize="28" FontWeight="Bold" TextWrapping="Wrap" Margin="0,20,0,30"/>
        </StackPanel>

        <!-- Options List -->
        <ListBox Grid.Row="1" ItemsSource="{Binding CurrentOptions}" SelectedItem="{Binding SelectedOption}"
                 BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <RadioButton Content="{Binding Text}" IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                 FontSize="18" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Next" Command="{Binding NextCommand}" Margin="0,0,10,0" Padding="20,10"/>
            <Button Content="Submit Test" Command="{Binding SubmitCommand}" Padding="20,10" Background="{DynamicResource Accent.PrimaryBrush}" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>