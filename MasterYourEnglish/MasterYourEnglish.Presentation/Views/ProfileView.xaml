<UserControl x:Class="MasterYourEnglish.Presentation.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vms="clr-namespace:MasterYourEnglish.Presentation.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vms:ProfileViewModel}"
             d:DesignHeight="700" d:DesignWidth="900"
             Background="{DynamicResource App.BackgroundBrush}">

    <UserControl.Resources>
        <!-- Section Card Style -->
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Sidebar.BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border.PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="30"/>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Form Label Style -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
        </Style>

        <!-- Form Input Style -->
        <Style x:Key="FormInput" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="{DynamicResource App.BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border.PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="Background" Value="{DynamicResource Accent.PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Profile"
                       FontSize="28" FontWeight="Bold"
                       Foreground="{DynamicResource Text.PrimaryBrush}"/>
            <TextBlock Text="Manage your account settings"
                       FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Content Card -->
        <Border Grid.Row="1" Style="{StaticResource SectionCard}" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Profile Sidebar (Image + Name) -->
                <StackPanel Grid.Column="0" Margin="0,0,40,0" Width="220">
                    <Border Width="200" Height="200" 
                            Background="{DynamicResource App.BackgroundBrush}"
                            BorderBrush="{DynamicResource Border.PrimaryBrush}"
                            BorderThickness="1" CornerRadius="8">
                            <!-- Placeholder for Profile Image if needed -->
                            <TextBlock Text="👤" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.2"/>
                    </Border>

                    <TextBlock Text="{Binding FullName}" HorizontalAlignment="Center" 
                               FontSize="18" FontWeight="SemiBold" Margin="0,15,0,2"
                               Foreground="{DynamicResource Text.PrimaryBrush}" TextWrapping="Wrap" TextAlignment="Center"/>
                    <TextBlock Text="{Binding Username}" HorizontalAlignment="Center" 
                               FontSize="14" Foreground="Gray"/>
                </StackPanel>

                <!-- Settings Form -->
                <StackPanel Grid.Column="1" MaxWidth="600" HorizontalAlignment="Left">
                    <TextBlock Text="👤 Personal Information" Style="{StaticResource SectionHeader}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="First Name" Style="{StaticResource FormLabel}"/>
                            <TextBox Text="{Binding FirstName}" Style="{StaticResource FormInput}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Last Name" Style="{StaticResource FormLabel}"/>
                            <TextBox Text="{Binding LastName}" Style="{StaticResource FormInput}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Username" Style="{StaticResource FormLabel}"/>
                    <TextBox Text="{Binding Username}" 
                             IsReadOnly="True" 
                             Style="{StaticResource FormInput}"
                             Background="{DynamicResource Sidebar.HoverBrush}" Opacity="0.8"/>

                    <TextBlock Text="Email" Style="{StaticResource FormLabel}"/>
                    <TextBox Text="{Binding Email}" 
                             IsReadOnly="True" 
                             Style="{StaticResource FormInput}"
                             Background="{DynamicResource Sidebar.HoverBrush}" Opacity="0.8"/>

                    <Button Content="Save Changes" Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveChangesCommand}"
                            HorizontalAlignment="Left" MinWidth="160" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>