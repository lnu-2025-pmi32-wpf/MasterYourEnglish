<UserControl x:Class="MasterYourEnglish.Presentation.Views.SavedFlashcardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vms="clr-namespace:MasterYourEnglish.Presentation.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vms:SavedFlashcardsViewModel}"
             d:DesignHeight="700" d:DesignWidth="900"
             Background="{DynamicResource App.BackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="DifficultyTagText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="DifficultyTagStyle" TargetType="Border">
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Sidebar.BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border.PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="215"/>
            <Setter Property="Height" Value="250"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter Margin="15"
                                              VerticalAlignment="Top"
                                              HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background"
                                        Value="{DynamicResource Sidebar.HoverBrush}"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush"
                                        Value="{DynamicResource Accent.PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DetailPanelStyle" TargetType="Grid">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedFlashcard}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- New Button Styles for Description Panel -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Background" Value="{DynamicResource Accent.PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RemoveButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#EF4444"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="28" FontWeight="SemiBold" 
                   Foreground="{DynamicResource Text.PrimaryBrush}" Margin="0,0,0,20">
            <Run Text="Flashcards"/>
            <Run Text="→ Saved" Foreground="Gray"/>
        </TextBlock>

        <Grid Grid.Row="1" Margin="0,-10,0,10">
            <Button Style="{StaticResource CardButtonStyle}" 
            HorizontalAlignment="Left"
            Command="{Binding StartTestYourselfCommand}">
                <StackPanel>
                    <TextBlock Text="Test yourself" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    <TextBlock Text="Generate a test from your saved flashcards." FontSize="14" TextWrapping="Wrap" Margin="0,10,0,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="16" Padding="5"
                     MaxWidth="500" HorizontalAlignment="Left" MinWidth="400"
                     VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" 
                      ItemsSource="{Binding SortOptions}"
                      SelectedItem="{Binding SelectedSortOption}"
                      FontSize="16" Padding="5"
                      Margin="10,0,10,0" MinWidth="150" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="Filter" FontSize="16" Padding="10,5"
                    MinWidth="100" VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Styled List View -->
            <Border Grid.Column="0" 
                    Background="{DynamicResource Sidebar.BackgroundBrush}"
                    BorderBrush="{DynamicResource Border.PrimaryBrush}" BorderThickness="1"
                    CornerRadius="8">
                <ListView Margin="2"
                          ItemsSource="{Binding SavedFlashcards}"
                          SelectedItem="{Binding SelectedFlashcard}"
                          Background="Transparent" BorderThickness="0"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Word}" FontSize="16"  FontWeight="Medium" Padding="10,8"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" 
                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Sidebar.HoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Accent.PrimaryBrush}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource Text.OnAccentBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>

            <!-- Styled Detail Panel -->
            <Grid Grid.Column="1" Margin="30,0,0,0" 
                  Style="{StaticResource DetailPanelStyle}">
                <Border Background="{DynamicResource Sidebar.BackgroundBrush}" 
                        BorderBrush="{DynamicResource Border.PrimaryBrush}" BorderThickness="1"
                        CornerRadius="8" Padding="30">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding SelectedFlashcard.Word}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource Text.PrimaryBrush}"/>
                                <Border Style="{StaticResource DifficultyTagStyle}" Margin="15,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding SelectedFlashcard.Difficulty}" Style="{StaticResource DifficultyTagText}"/>
                                </Border>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,20">
                                <TextBlock Text="{Binding SelectedFlashcard.PartOfSpeech}" FontSize="16" FontStyle="Italic" Foreground="Gray"/>
                                <TextBlock Text=" • " FontSize="16" Foreground="Gray">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedFlashcard.Transcription}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SelectedFlashcard.Transcription}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding SelectedFlashcard.Transcription}" FontSize="16" Foreground="Gray"/>
                            </StackPanel>

                            <TextBlock Text="Meaning" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource Accent.PrimaryBrush}" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding SelectedFlashcard.Meaning}" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="26"/>

                            <TextBlock Text="Example" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource Accent.PrimaryBrush}" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding SelectedFlashcard.Example}" FontSize="16" FontStyle="Italic" TextWrapping="Wrap" Margin="0,0,0,30" LineHeight="24"/>

                            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                                <Button Content="Mark as Learned" 
                                        Command="{Binding MarkAsLearnedCommand}"
                                        Style="{StaticResource PrimaryButton}"/>
                                <Button Content="Remove" Margin="15,0,0,0" 
                                        Command="{Binding RemoveFromSavedCommand}"
                                        Style="{StaticResource RemoveButton}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>