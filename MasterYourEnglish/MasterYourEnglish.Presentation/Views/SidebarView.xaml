<UserControl x:Class="MasterYourEnglish.Presentation.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vms="clr-namespace:MasterYourEnglish.Presentation.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="240"
             d:DataContext="{d:DesignInstance Type=vms:SidebarViewModel}"
             Background="{DynamicResource Sidebar.BackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="NavButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Accent.PrimaryBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource Text.OnAccentBrush}"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True" />
                        <Condition Property="IsChecked" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource Sidebar.HoverBrush}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CommandButtonStyle" TargetType="Button" >
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Sidebar.HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MasterYourEnglish" 
                   FontSize="22" FontWeight="Bold" 
                   Foreground="{DynamicResource Text.PrimaryBrush}"
                   Margin="15,20"/>

        <StackPanel Grid.Row="1" Margin="0,15,0,0">
            <RadioButton Content="Profile" Style="{StaticResource NavButtonStyle}" GroupName="NavLinks"
                         Command="{Binding NavigateToProfileCommand}" IsChecked="True"/>

            <RadioButton Content="Flashcards" Style="{StaticResource NavButtonStyle}" GroupName="NavLinks" 
                         Command="{Binding NavigateToFlashcardsCommand}"/>

            <RadioButton Content="Tests" Style="{StaticResource NavButtonStyle}" GroupName="NavLinks" 
                         Command="{Binding NavigateToTestsCommand}"/>

            <RadioButton Content="Statistics" Style="{StaticResource NavButtonStyle}" GroupName="NavLinks"
                         Command="{Binding NavigateToStatisticsCommand}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <Button Content="Settings" Style="{StaticResource CommandButtonStyle}"
                    Command="{Binding NavigateToSettingsCommand}"/>

            <Button Content="Logout" Style="{StaticResource CommandButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>